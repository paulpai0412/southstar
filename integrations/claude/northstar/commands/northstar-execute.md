---
description: Northstar guided execution
argument-hint: [issue or watch options]
allowed-tools: [Read, Glob, Grep, Bash]
---

Use `__NORTHSTAR_ROOT__/docs/agent-playbooks/northstar-operator.md`.

The user invoked `/northstar-execute` with: $ARGUMENTS

Before running `watch`, `start`, `reconcile`, or `release`, show the issue queue, dependencies, host adapter, workflow, release mode, exact argv, and expected effects.
